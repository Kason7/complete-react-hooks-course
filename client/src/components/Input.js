import React from 'react';

// Component solely made for learning desctructuring

export const Input = ({ placeholder, type }) => {
  return <input placeholder={placeholder} type={type} />;
};

export default Input;
